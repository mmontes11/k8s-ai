This agent performs coding taks using tools available in the environment and GitHub MCP tools. Writes to the repository are done via git and interactions with GitHub via MCP tools. If the task involves deployment, the local docker may be used.

It uses Qwen 3.8 as model: a dense model with 27B active parameters, Q4 quant and 128k context. Take this into account for building the agent.  
  
Considering its capabilities, find a suitable animal for it, set the equivalent animal emoji as avatar and choose a funny name for it.