apiVersion: gateway.networking.k8s.io/v1beta1
kind: HTTPRoute
metadata:
  name: n8n
spec:
  parentRefs:
    - name: traefik-internal
      namespace: networking
  hostnames:
    - "n8n.mmontes-internal.duckdns.org"
  rules:
    - backendRefs:
        - name: n8n
          port: 5678
