apiVersion: gateway.networking.k8s.io/v1beta1
kind: HTTPRoute
metadata:
  name: mcp-grafana
spec:
  parentRefs:
    - name: traefik-internal
      namespace: networking
  hostnames:
    - "mcp-grafana.mmontes-internal.duckdns.org"
  rules:
    - backendRefs:
        - name: mcp-grafana
          port: 8000